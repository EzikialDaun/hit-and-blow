# 리스트에 중복된 값이 있는지 확인
# data는 리스트 변수
# 중복된 값이 없으면(고유하면) True 리턴
# 중복된 값이 있으면(고유하지 않으면) False 리턴
# ex) [0, 1, 2, 3]      ==>     True
# ex) [0, 0, 1, 2]      ==>     False
def is_unique(data):
    # 코드 작성

print(is_unique([0, 1, 2, 3]))
print(is_unique([0, 0, 1, 2]))
